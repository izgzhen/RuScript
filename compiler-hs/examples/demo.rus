# Demo of RuScript


class example {
    fn adder(x, y) {
        return x + y;
    }
};

obj = new example;

a = 1;
b = 2;
c = obj.adder(a, b);
print c;



# class example2 {
#     fn bridge(x, y) {
#         global obj;
#         return obj(x, y);
#     }
# }

# obj2 = new example2

# c = obj2.bridge(a, b) # c should be 3

